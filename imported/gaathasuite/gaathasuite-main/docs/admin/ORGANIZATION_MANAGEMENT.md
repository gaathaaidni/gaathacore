# Organization Management

**Status:** PARTIALLY IMPLEMENTED.

Organization IDs are associated with users and business models. Admin operations must derive organization context server-side. Cross-tenant administration is not a supported assumption and requires negative tests before production claims.
