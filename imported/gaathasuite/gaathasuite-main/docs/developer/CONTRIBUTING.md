# Contributing

**Status:** IMPLEMENTED as repository guidance.

Keep changes focused, preserve existing behavior, and update documentation when implementation changes. Do not commit secrets. For security-sensitive changes, add negative tests for authentication, role, organization ownership, and destructive actions. Run the focused test/build command and `git diff --check` before review.
