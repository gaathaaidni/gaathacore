# Monitoring

**Status:** PARTIALLY IMPLEMENTED.

Use process/container health, `/health`, `/ready`, database health, Redis health, CPU, memory, disk, Nginx/TLS, error logs, and login smoke tests. Centralized metrics, alert thresholds, and an on-call service are not evidenced and should be configured before stronger availability claims.
