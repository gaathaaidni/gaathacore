apiVersion: v1
kind: Secret
metadata:
  name: gaatha-secrets
type: Opaque
data:
  # Replace these base64 values with your real secrets (base64-encoded)
  DATABASE_URL: ""  # e.g. base64 of 'postgresql+psycopg2://user:pass@host:5432/dbname'
  SECRET_KEY: ""    # base64 of your secret key
