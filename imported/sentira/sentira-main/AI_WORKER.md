# Sentira AI Worker

The Python AI worker normalizes detections and tracking output before publishing to the rule engine path. Phase 6 validation can use generated or sample RTSP/MP4 streams when physical CCTV hardware is unavailable; physical-camera verification must not be claimed unless performed.
